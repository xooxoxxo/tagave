{
  "name": "@liner/worker",
  "version": "0.1.0",
  "type": "module",
  "description": "Liner music archive worker: scanning, clustering, matching, enrichment, tag writing",
  "_notes": {
    "python": "The tag writer sidecar requires Python 3.13+ with mutagen >=1.47. Set LINER_TAGWRITER_PYTHON env var to override the interpreter path, or use .venv/bin/python in packages/tagwriter-py."
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "dev": "node --watch --loader tsx dist/index.js"
  },
  "dependencies": {
    "@liner/core": "workspace:*",
    "@liner/db": "workspace:*",
    "@liner/shared": "workspace:*",
    "drizzle-orm": "^0.36.4",
    "music-metadata": "^11.9.0",
    "pg-boss": "^11.1.1",
    "pino": "^9.5.0",
    "pino-pretty": "^10.3.1",
    "postgres": "^3.4.4",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.2"
  },
  "engines": {
    "node": ">=22.12"
  }
}