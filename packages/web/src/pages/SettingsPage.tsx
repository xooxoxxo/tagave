/**
 * Settings page: library, providers, collection, system tabs
 * To be filled by another agent
 */

import { PageShell } from '../components/ui';

export function SettingsPage() {
  return (
    <PageShell title="Settings" subtitle="Configure your library and application">
      <div>Settings content to be filled by another agent</div>
    </PageShell>
  );
}
